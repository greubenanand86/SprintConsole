# SprintConsole Tasks

Individual task files for SprintConsole. Each file is one task.

## Naming

```
SC-{NNN}-{short-slug}.md
```

## Status

Managed via the Kanban board at `products/sprintconsole/kanban.md`.
Drag cards between columns in Obsidian — the markdown updates automatically.

## Creating a task

Use the Obsidian template: `obsidian-templates/task.md`
Or run: `TASK_WRITE_ENABLED=true .claude/hooks/pm-agent.sh`
